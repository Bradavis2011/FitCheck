{
  "name": "orthis-monorepo",
  "version": "1.0.0",
  "description": "Or This? - AI-powered outfit feedback app",
  "private": true,
  "scripts": {
    "dev": "cd fitcheck-app && npm run dev",
    "start": "npm run dev",
    "backend": "cd fitcheck-api && npm run dev",
    "frontend": "cd fitcheck-app && npm start",
    "install-all": "npm install && cd fitcheck-app && npm install && cd ../fitcheck-api && npm install",
    "clean": "cd fitcheck-app && rm -rf node_modules && cd ../fitcheck-api && rm -rf node_modules"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
